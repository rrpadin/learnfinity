# Learnfinity Mobile

This Expo app is the starting point for Learnfinity on iOS and Android.

## Run locally

```bash
cp .env.example .env
npm install
npm start
```

Set `EXPO_PUBLIC_POCKETBASE_URL` to a reachable PocketBase base URL for your phone or simulator.
